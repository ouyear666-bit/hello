# 运动手表 项目文档

## 一、硬件清单

| 模块 | 型号 | 说明 |
|------|------|------|
| 主控 | STM32F103C8T6 | Cortex-M3, 72MHz, 64KB Flash, 20KB RAM |
| 屏幕 | 1.3寸 OLED | SH1106 驱动, 128×64 分辨率, I2C 接口 |
| 传感器 | MPU6050 | 6轴陀螺仪+加速度计, I2C 接口 |
| 蓝牙 | BLE 串口模块 | 兼容 JDY/HM/CC 系列 BLE 透传模块 |
| 编码器 | EC11 旋转编码器 | 带按键, 用于页面切换和设置操作 |
| RTC | 板载 RTC | STM32 内置 RTC, LSI 时钟源 |

## 二、引脚接线

### I2C 总线（共享）
| STM32 引脚 | 连接设备 |
|-----------|---------|
| PB6 (SCL) | OLED SCL + MPU6050 SCL |
| PB7 (SDA) | OLED SDA + MPU6050 SDA |

### OLED
| OLED 引脚 | 连接 |
|----------|------|
| VCC | 3.3V |
| GND | GND |
| SCL | PB6 |
| SDA | PB7 |

### MPU6050
| MPU6050 引脚 | 连接 |
|-------------|------|
| VCC | 3.3V |
| GND | GND |
| SCL | PB6 |
| SDA | PB7 |
| AD0 | GND（地址 0x68）|

### 旋转编码器 EC11
| EC11 引脚 | STM32 引脚 |
|----------|-----------|
| A 相 | PA0 |
| B 相 | PA1 |
| 按键 | PA2 |
| GND | GND |

### BLE 蓝牙模块
| BLE 引脚 | STM32 引脚 |
|---------|-----------|
| TXD | PA10 (USART1 RX) |
| RXD | PA9 (USART1 TX) |
| VCC | 3.3V |
| GND | GND |

## 三、软件架构

```
运动手表/SportsWatch/
├── Core/
│   ├── Inc/                    # 头文件
│   │   ├── main.h              # HAL 主头文件
│   │   ├── oled.h              # OLED 驱动 (128×64, SH1106)
│   │   ├── mpu6050.h           # MPU6050 传感器驱动
│   │   ├── pedometer.h         # 计步算法
│   │   ├── ui.h                # 用户界面
│   │   ├── encoder.h           # EC11 编码器驱动
│   │   ├── bt.h                # 蓝牙通信模块
│   │   ├── cn_font.h           # 中文字库(16×16)
│   │   ├── rtc.h               # RTC 时钟
│   │   ├── i2c.h               # I2C 驱动
│   │   └── usart.h             # 串口驱动
│   └── Src/                    # 源文件
│       ├── main.c              # 程序入口, FreeRTOS 启动
│       ├── freertos.c          # 主任务 (Task_Main)
│       ├── oled.c              # OLED 显示驱动
│       ├── mpu6050.c           # MPU6050 I2C 读写
│       ├── pedometer.c         # 计步算法实现
│       ├── ui.c                # 界面渲染 + 事件处理
│       ├── encoder.c           # 编码器轮询
│       ├── bt.c                # 蓝牙数据上传
│       ├── i2c.c               # I2C1 初始化
│       ├── usart.c             # USART1 (9600bps)
│       ├── rtc.c               # RTC 初始化
│       └── stm32f1xx_it.c      # 中断服务
├── Drivers/                    # HAL 库 + CMSIS
├── Middlewares/                # FreeRTOS
└── MDK-ARM/                    # Keil 工程文件
```

## 四、功能说明

### 主界面
启动后显示系统自检，检测 OLED 和 MPU6050 状态，通过后进入主界面。

### 时间页面
- 显示：日期（月/日/周几）+ 时间（时:分）+ 温度
- **长按编码器** → 进入时间设置（月→日→周→时→分，旋转调值，短按切换，最后一个字段保存）
- **旋转编码器** → 切换到运动页面
![alt text](b087be3b65a7dab8298a346d90c09ac5.jpg)
### 运动页面
- 显示：步数 / 距离 / 卡路里
- **长按编码器** → 清除运动数据（步数归零）
- **旋转编码器** → 切换到时间页面
![alt text](cf96a22ac52c6f8272f15687d343f147.jpg)
### 计步算法（角度双沿计步算法）
- 基于MPU6050六轴传感器，`atan2(ax, -ay)`加速度矢量角度特征提取
- 双沿滞回状态机步态识别（45°抬臂阈值 / 10°放臂阈值）
- 180ms最小间隔滤波抑制高频抖动噪声
- 2秒静止超时自动复位状态机，杜绝静态误计数
- 25Hz传感器采样，结合FreeRTOS任务时序稳定运行

### 蓝牙上传
- 运动页面每 2 秒通过 BLE 串口发送数据
- 格式：`S:42 D:7 C:0.3 T:25.5\r\n`
  - S = 步数, D = 距离(米), C = 卡路里(kcal), T = 温度(°C)

## 五、编译与烧录

### 固件编译
1. 用 Keil MDK-ARM 5 打开 `运动手表/SportsWatch/MDK-ARM/SportsWatch.uvprojx`
2. 编译目标：`SportsWatch`
3. 编译输出：`SportsWatch.hex`

### 烧录工具
- ST-Link V2 或 USB-TTL 串口烧录
- 芯片：STM32F103C8

## 六、运动手表 PC端上位机

基于 Python 的桌面应用程序，通过 BLE 蓝牙连接 STM32 运动手表，实时显示运动数据。

## 功能

| 页面 | 功能 |
|------|------|
| ⏰ **时间** | 显示当前日期、时间、手表温度 |
| 🏃 **运动** | 显示步数、距离、卡路里，支持一键清零 |
| ❤️ **心率** | 显示实时心率（需手表支持 H 字段协议） |
| 📋 **历史** | 自动记录运动数据，可清除 |
![alt text](<屏幕截图 2026-07-10 010853.png>)
![alt text](<屏幕截图 2026-07-10 010859.png>)
![alt text](<屏幕截图 2026-07-10 010905.png>)
![alt text](<屏幕截图 2026-07-10 010910.png>)
## 安装与运行

### 安装依赖

```bash
pip install bleak pillow
```

### 运行程序

```bash
python main.py
```

## 蓝牙协议

手表通过 BLE 发送数据格式：

```
S:42 D:7 C:0.3 T:25.5
```

| 字段 | 含义 | 单位 | 示例 |
|------|------|------|------|
| S | 步数 | 步 | 42 |
| D | 距离 | 米 | 7 |
| C | 卡路里 | kcal | 0.3 |
| T | 温度 | °C | 25.5 |
| H | 心率(扩展) | BPM | 72 |

## 系统要求

- Windows 10/11（需支持 BLE 蓝牙 4.0+）
- Python 3.8+
## 七、数据格式

### 蓝牙通信协议
```
上行（手表 → 手机）：每 2 秒一条
格式：S:{步数} D:{距离米} C:{卡路里} T:{温度℃}\r\n
示例：S:42 D:7 C:0.3 T:25.5
```

### 字段说明
| 字段 | 含义 | 单位 | 示例 |
|------|------|------|------|
| S | 当日累计步数 | 步 | 42 |
| D | 距离 | 米 | 7 |
| C | 卡路里 | kcal | 0.3 |
| T | 温度 | °C | 25.5 |

## 八、编码器操作一览

| 页面 | 操作 | 功能 |
|------|------|------|
| 任意 | 旋转 | 切换时间/运动页面 |
| 时间页 | 长按 | 进入时间设置 |
| 运动页 | 长按 | 清除运动数据 |
| 设置中 | 旋转 | 调整数值 |
| 设置中 | 短按 | 下一字段 |
| 设置中 | 长按 | 取消返回 |
